<%= form_for(@paypal_payment) do |f| %>
  <% if @paypal_payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@paypal_payment.errors.count, "error") %> prohibited this paypal_payment from being saved:</h2>

      <ul>
      <% @paypal_payment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :payer_id %><br>
    <%= f.text_field :payer_id %>
  </div>
  <div class="field">
    <%= f.label :token %><br>
    <%= f.text_field :token %>
  </div>
  <div class="field">
    <%= f.label :profile_id %><br>
    <%= f.text_field :profile_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
